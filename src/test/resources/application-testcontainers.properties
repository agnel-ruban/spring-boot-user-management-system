# Testcontainers Profile Configuration
# This profile uses real PostgreSQL in Docker containers for testing

# Database Configuration - Will be overridden by Testcontainers
spring.datasource.url=${spring.datasource.url}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable Liquibase for testcontainers
spring.liquibase.enabled=false

# Testing specific settings
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration for Testing
jwt.secret=dGVzdC1zZWNyZXQta2V5LWZvci10ZXN0Y29udGFpbmVycy10ZXN0aW5nLW9ubHktbm90LWZvci1wcm9kdWN0aW9uLXVzZQ==
jwt.expiration=3600000
